# Liveness Examples in Ivy

Case studies: `LIVENESS_GUIDE.md` (the method), `LIVENESS_CASE_STUDY_GERMAN.md` (German protocol), `LIVENESS_CASE_STUDY_MESI.md` (MESI protocol).

| File Name | Description | Property | Fairness Conditions | Rule | Remarks | Model OK? | Proof OK? | Manual Automation OK?| Auto Automation OK?|
|-----------|-------------|----------|---------------------|------|---------|-----------|-----------|---------------|----------|
|`one_queue.ivy`| Single unbounded FIFO queue, non-consecutive issue timestamps | $\square\forall X. begun(X) -> \Diamond done(X)$   | $\square \diamond recv$ | AUTO2 | n/a | Yes | Yes | Yes | No |
| `one_queue_unord.ivy` | Unbounded unordered channel | same as above | $\forall X. \square\diamond recv(X)$ | AUTO2 | n/a | Yes | Yes | Yes | No |
|`pingpong1.ivy`| Single unbounded FIFO queue, consecutive issue timestamps | same as above | same as above | AUTO2 | n/a | Yes | Yes | Yes | No |
|`pingpong2.ivy`| Blocking unbouned FIFO queue, at most one element in flight | same as above | same as above | AUTO2 | n/a | Yes | Yes | Yes | No |
|`pingpong3.ivy`| Blocking unbounded FIFO queue with invariant showing at most one element in flight | same as above | same as above | finite | n/a | Yes | Yes | Yes | No |
|`two_queues.ivy`|Two FIFO queues concatenated | $\square \forall X. begun1(X) -> \Diamond done2(X)$ | $\square\Diamond recv1 \wedge \square\Diamond recv2$ | AUTO5 | n/a | Yes | Yes | Yes | No |
|`two_colored_queue.ivy`| FIFO queue with each message having two types. There are two corresponding polling operations, blocking based on message type. | $\square \forall X. begun(X) -> \Diamond done(X)$ | $\square\Diamond recv $ | AUTO5 | n/a | Yes | Yes | Somewhat | No | 
| `token_ring.ivy` | Token ring mutex protocol modeled as a fixed linked list | ... | ... | LEX | ... | Yes | Yes | Yes | No |
| `token_ring_btw.ivy` | Token ring mutex protocol modeled using the between relation and no witness for token holder | ... | ... | AUTO2 | n/a | Yes | Yes | Yes | No |
| `token_ring_btw_instr.ivy` | Token ring mutex protocol modeled using between relation and witness for token holder | ... | ... | AUTO2 | n/a | Yes | Yes | Yes | No |
| `ticket.ivy` | Ticket mutex protocol | ... | ... | AUTO5 | n/a | Yes | Yes | Yes | No | 
| `ticket_ranking.ivy` | Ticket mutex protocol | ... | ... | LEX | n/a | Yes | Yes | Yes | No |
| `german_3channels_original.ivy` | German protocol, one home + n clients, three unit-capacity channels per client | $\square\forall C. waiting(C) \to \Diamond \neg waiting(C)$ (every request gets a response) | $\forall C.\ \square\diamond wf\_x(C)$ for every rule, **plus strong fairness (compassion) $(\square\diamond E(C)) \to (\square\diamond f\_pick(C))$ for the request arbiter** | AUTO5 | Proved via lemma `home_idle` (home always finishes the current command) chained into the main property; stage 1 uses the `strongfair.ivy` tableau case split. Weak fairness alone is insufficient -- the arbiter can starve a client. | Yes | Yes | Yes | No |
| `german_3channels_lex.ivy` | Same model as `german_3channels_original.ivy` | same | same | **LEX** | Single `ranking` call, 11 lexicographic components, **no auxiliary lemma**. Stage 1 ([00],[01]) and stage 3 ([02],[03]) preempt the home's invalidation pipeline ([04]..[10]), which is what lets the pipeline grow while other clients are being served -- the obligation that forces the two-step proof under Rule 8. Ablations confirm the order is load-bearing: demoting stage 1 or stage 3, or switching to `l2s_auto5`, breaks it. Note: suffixes sort as strings, hence `[00]`..`[10]`. | Yes | Yes | Yes | No |
| `german_3channels_fifo.ivy` | German protocol with a **FIFO arbiter**: requests are timestamped and the home serves the oldest | same as above | $\square\diamond wf\_x$ for every rule -- **weak fairness only, no compassion** | **LEX** | Single `ranking` call, 10 lexicographic components, no lemma and **no temporal operator anywhere in the proof**. Top component is the CAV'24 "pending timestamps $\le$ mine" ranking, carried on clients so `work_created = true`. Pays for liveness in the model rather than in the assumption -- incomparable to the other two files. Ablation: an arbitrary arbiter breaks `l2s_progress[00]`. | Yes | Yes | Yes | No |
| `mesi.ivy` | **MESI** cache coherence (M/E/S/I), one snooping bus controller + n caches, three unit-capacity channels per cache, split-transaction bus | *safety only*: $\square$ `swmr` (an M or E copy is the only copy) $\wedge$ `data_coh` (every clean valid copy equals memory) | n/a | n/a | Split-transaction snooping bus, **no directory**: the controller broadcasts to all caches and learns who holds the line from the snoop responses (`ctl_shared` = the MESI shared line, which decides E vs S). Snoops and grants share one channel, which is what serialises the protocol -- `grant_blocks`/`one_grant` are load-bearing for `swmr`. Models the silent E→M upgrade and BusRd→E, both shown reachable. | Yes | Yes | Yes | n/a |
| `mesi_live.ivy` | Same model as `mesi.ivy` (data dropped) + **FIFO bus arbitration** | $\square\forall C. waiting(C) \to \Diamond \neg waiting(C)$ (every memory access is eventually served), **plus** the full coherence theorem | $\square\diamond wf\_x$ for every rule -- **weak fairness only, no compassion** | **LEX** | Single `ranking` call, 12 lexicographic components, no lemma and **no temporal operator anywhere in the proof**. `[00]` is the CAV'24 "pending timestamps $\le$ mine" ranking carried on clients; `[01]`..`[03]` are the three MESI grant flavours; `[04]`..`[11]` are the transaction pipeline, including three "stale grant drain" stages that exist because snoops and grants share a channel. Ablations: an arbitrary arbiter breaks `l2s_progress[00]`; `l2s_auto5` breaks 16 checks; demoting `[00]` or a stage-3 component breaks `l2s_needed_preserved[04..11]`. | Yes | Yes | Yes | No |
| `mesi_live_compassion.ivy` | Same model with an **arbitrary** arbiter (as MESI is usually specified) | same as above | weak fairness everywhere **+ compassion for the bus arbiter** | **LEX** | Single `ranking` call, 13 components. Stage 1 needs the `strongfair.ivy` three-way tableau case split, plus the compassion axiom re-stated as an `invariant` inside the block. Incomparable to `mesi_live.ivy`: weaker model, stronger assumption. Replacing compassion by weak fairness leaves **exactly one** failing check, `l2s_sched_stable[01]`, whose CTI is the two-client starvation scenario. | Yes | Yes | Yes | No |
| `mesi_split_bus_unsafe.ivy` | **Counterexample artifact (expected to FAIL).** `mesi.ivy` with the single downward channel split into a separate **address bus** (`chan_addr`, snoops) and **data bus** (`chan_data`, grants), as a real split-transaction bus has. One guard changes. | *safety*: `swmr` -- **violated** | n/a | BMC[14] | Answers "does the model distinguish address/data/shared buses?" -- it does not, and this file shows the fusion is load-bearing. `ivy_check` reports `BMC with bound 14 found a counter-example`: cache 0 is granted **E**, and before it installs the grant a second transaction snoops it; still `invalid`, it answers "no copy" about a line it already owns, so cache 1 is granted **M**. Final state `cache(0)=exclusive & cache(1)=modified`. A machine with independent address and data buses needs a snoop-pending stall / retry / fill-buffer check that this model lacks. | Yes (deliberately unsafe) | n/a | n/a | n/a |
| `german_nchan_1.ivy` | German Protocol with n single-capacity channels | ... | ... | LEX | n/a | ? | ? | ? | ? |
| `german_1chan_n.ivy` | German Protocol with 1 unbounded FIFO channel | ... |... | LEX | n/a | ? | ? | ? | ? |
| `german_nchan_n.ivy` | German Protocol with n unbounded FIFO channels | ...|... | LEX | n/a | ? | ? | ? | ? |
| `moesi.ivy` | **MOESI** cache coherence protocol, directory based: one home + n caches, three unit-capacity channels per cache, five cache states (M/O/E/S/I), two probe kinds (`invalidate` / `downgrade`) and three grant kinds. Safety only. | The MOESI state **compatibility table**: at most one M, at most one E, both exclude every other copy; at most one O, compatible only with S and I | n/a (safety) | n/a | 12 supporting directory-consistency invariants. Only one differs in shape from the German originals: `shared_implies_no_writer` needs the escape clause `\| channel3(C) = downgradeAck`, because the E -> S downgrade makes the cache read "shared" while the directory still reads "writable" until the ack lands -- a window German does not have. Dirty sharing (O + S) confirmed reachable by an 11-step `bmc[14]` trace. | Yes | Yes | Yes | n/a |
| `moesi_live.ivy` | Same model + a **FIFO arbiter** (timestamped requests, home serves the oldest) + ghost `waiting(C)` | $\square\forall C. waiting(C) \to \Diamond \neg waiting(C)$ (every memory access is eventually served) | $\square\diamond wf\_x$ for every rule -- **weak fairness only, no compassion** | **LEX** | Single `ranking` call, 16 lexicographic components, no lemma and **no temporal operator anywhere in the proof**; verified on the first `ivy_check` run. MOESI needs **two** probe pipelines where German needs one: [04]-[06] invalidate (ranked on "N is still a sharer") and [07]-[09] downgrade (ranked on "write permission is still outstanding") -- a downgrade removes nobody from the sharer list, which is exactly the point of the O state. Three grant kinds give three stage-3 components and three stale-grant drains. Measured: the load-bearing order is **one cut only** -- {stage 1, stage 3} above {the whole stage-2 pipeline}; reversing the entire 12-component pipeline still verifies, demoting any single stage-1/stage-3 component does not. Ablation: an arbitrary arbiter breaks `l2s_progress[00]`; `l2s_auto5` breaks 24 checks. | Yes | Yes | Yes | No |
| `moesi_live_compassion.ivy` | Same model with the **arbitrary arbiter of MOESI as specified** (generated from `moesi_live.ivy` by `gen_moesi_compassion.py`) | same | same, **plus strong fairness (compassion) $(\square\diamond E(C)) \to (\square\diamond f\_pick(C))$ for the request arbiter** | **LEX** | 17 components. Stage 1 splits into the `strongfair.ivy` three-way tableau case analysis ([00] E finitely often, [01] E infinitely often, "E never again" handled by the pipeline). Compassion has to be re-stated as an `invariant` inside the ranking block, since `instantiate` only pins it at time 0. Incomparable to `moesi_live.ivy`: weaker model, stronger assumption. Ablation: swapping compassion for weak fairness of the pick breaks `l2s_progress[01]`. | Yes | Yes | Yes | No |
| `abp_ranking_first_order.ivy` | ABP with an instrumented **fair-lossy** channel: a message may be lost only while an older one is still in flight ahead of it, and both receive actions are guarded commands with turn flags | same as `abp_ranking.ivy` | $\square\diamond$ of four scheduler turn flags — **plain weak fairness only, no conditional assumption** | **LEX** | Single `ranking` call, 6 lexicographic components, **zero temporal operators anywhere in the proof** (`abp_ranking.ivy` has 16). Head protection makes both "channel non-empty" and "a stale message is present" stable, which also retires the ghost drain signal. Incomparable to `abp_ranking.ivy`: stronger model assumption, weaker fairness. Channel is still genuinely lossy (checked). | Yes | Yes | Yes | No |
| `abp_ranking.ivy` | Alternating bit protocol (lossy unbounded FIFO channels), same model as `abp.ivy`/`abp_l2s.ivy` | $\forall I.\ \square(sender\_array(I)\neq\bot \to \Diamond receiver\_array(I)\neq\bot)$ | $\square\diamond sender\_scheduled$, $\square\diamond receiver\_scheduled$, and conditional channel fairness $(\square\diamond sent)\to(\square\diamond drained)$ for both channels | **LEX** | Single `ranking` call, 8 lexicographic components. Replaces the ~50 hand-written monitor invariants, witness constant and **four temporal-prophecy formulas** of `abp_l2s.ivy` — the bit-combination case analysis is subsumed by the component ordering. Components mix three sorts (`index_t`, `data_msg_t`, `ack_msg_t`, nullary). | Yes | Yes | Yes | No |
| `my_primary_backup.ivy` | Simple Primary/Backup Protocol|...|...|AUTO5|n/a|Yes|Yes|Yes|No|
| `two_phsae_commit.ivy` | Two-phase Commit Protocol | ... | ... | AUTO5 | n/a | Yes | Yes | Yes | No |
| `tomasulo_documented.ivy` | Tomasulo out-of-order execution engine (register renaming, reservation stations, common data bus), documented rewrite of `tomasulo.ivy`. Safety only. | Refinement to an in-order reference CPU: `~ir(R).resvd -> ir(R).val = ref.r(R)` and the observable `ensure opin = op_rd -> dout = refdout` | n/a (safety) | n/a | `tomasulo.ivy` as shipped is not checked at all (`attribute method = vmt`) and is outside FAU: the invariants it needs mention `st(K).opra.tag` under a universal `K`, a `tag_t -> tag_t` function, so Z3 can build an infinite instantiation chain. Fixed by relationalising the tags and the ALU. 8 inductive invariants + 3 derived ones, discharged in <1s. | Yes | Yes | Yes | n/a |
| `tomasulo_liveness.ivy` | Same machine, decomposed into guarded rules (`dispatch`/`issue`/`exec_finish`/`writeback`), per-station execution unit, ghost program-order timestamps and an `oldest` witness. | **[live_ready]** $\square\forall T.\ (valid(T)\wedge ops(T)) \to \Diamond \neg valid(T)$; **[live_nostarve]** $\square\forall T.\ valid(T) \to \Diamond \neg valid(T)$ (no instruction starves) | $\forall T.\ \square\diamond wf\_issue(T),\ wf\_exec(T),\ wf\_cdb(T)$ -- **weak fairness only, no compassion** | **LEX** | Two `ranking` calls, 3 lexicographic components each. Top component of [live_nostarve] is $A=\{M: valid(M)\wedge ts(M)\le ts(\_T)\}$; ghost timestamps are what make it conserved (a freshly dispatched station is younger than `_T`, so dispatch never grows it). `oldest` comes from `if some m. st_valid(m) minimizing st_ts(m)`, the sanctioned way to import "a finite non-empty set has a least element". **[live_nostarve] assumes one extra fact** -- "a station waiting for an operand is waiting for *somebody*" -- which is not EPR-expressible (it is $\forall tag\,\exists tag$, a self-loop; contrast `ticket_ranking.ivy`, where the same shape is fine because the quantifiers are over different sorts). Ablations: dropping it fails `l2s_sched_exists` and nothing else. | Yes | Yes | Yes | No |
| `msi.ivy` | **MSI cache coherence** over a *split-transaction snooping bus*: one line, n caches, three unit-capacity channels per cache (`reqchan` / `snpchan` / `rspchan`), broadcast snoops, silent + writeback eviction. Safety only; baseline for the two liveness files. | Coherence, both halves: **[swmr]** $st(C)=M \wedge C\ne D \to st(D)=I$ and **[dvi]** $st(C)\ne I \to val(C)=last\_val$ (every valid copy holds the latest written value) | n/a (safety) | n/a | 29 invariants, all inductive as written -- verified on the first run (377 checks, 3.7 s). A ghost `owner`/`dirty` witness keeps "some cache is in M" a *ground* term; written as $\forall C.\ st(C)\ne M$ in an antecedent it is an existential in positive position and leaves EPR. Reachability probed with deliberately-false invariants, including the starvation state (a cache queued while the bus serves another) and the blocking state (a stale response occupying the channel a snoop needs). | Yes | Yes | Yes | n/a |
| `msi_worked_example.ivy` | A four-phase execution of MSI (cold read; second reader; upgrade-and-store; read that forces a Flush), replayed against `msi.ivy` via `include`. Documentation, but *checked*. | Step-by-step `ensure`s on the cache states, memory and the dirty bit | n/a | n/a | Ivy treats a `require` in a **called** action as a proof obligation rather than an assumption, so every `call` re-proves that the rule was enabled -- the narrated trace in `LIVENESS_CASE_STUDY_MSI.md` §1.6 is therefore a theorem, not a hand-wave. Needs `require forall C. C = a | C = b | C = c` (exactly three caches): without it a fourth cache never gets snooped and `bus_complete` is never enabled, which `ivy_check` reports. | Yes | Yes | Yes | n/a |
| `msi_lex.ivy` | Same model, **arbitrary bus arbiter**, plus fairness instrumentation on the seven bus-side rules | $\square\forall C. waiting(C) \to \Diamond \neg waiting(C)$ (every read miss / write miss / upgrade is answered), plus the corollary **[request_answered]** over `reqchan` | $\forall C.\ \square\diamond wf\_x(C)$ for every bus rule, **plus compassion $(\square\diamond E(C)) \to (\square\diamond f\_arb(C))$ for the arbiter** | **LEX** | Single `ranking` call, 10 lexicographic components, no auxiliary lemma. Stage 1 ([00],[01] -- the `strongfair.ivy` tableau split of the compassion antecedent) and stage 3 ([02],[03]) preempt the bus's snoop sweep ([04]..[09]), which is what lets the sweep grow while other caches are served. Ablations: `l2s_auto5` fails 12, demoting stage 1 or stage 3 fails 6, **weak fairness for the arbiter fails `l2s_progress[01]`**. | Yes | Yes | Yes | No |
| `msi_fifo.ivy` | Same model with a **FIFO bus arbiter**: requests are timestamped and the (now unparameterized) arbiter serves the oldest | same as above | $\square\diamond wf\_x$ for every bus rule -- **weak fairness only, no compassion** | **LEX** | Single `ranking` call, 9 components, no lemma and **no temporal operator anywhere in the proof**. Top component is the CAV'24 "pending timestamps $\le$ mine" ranking, carried on *caches* so `work_created = true` stays a valid finite bound and all components share one sort. Incomparable to `msi_lex.ivy`, not stronger. Ablation: an arbitrary arbiter breaks `l2s_progress[00]`; `<` instead of `\le` breaks it too. | Yes | Yes | Yes | No |
| `lamport_mutex_live.ivy` | **Lamport's distributed mutual exclusion (DME)**, flattened from `lamport_mutex_man.ivy`: three message kinds (request/reply/release), a per-node request queue and reply-timestamp map, per-ordered-pair FIFO channels | $\square\forall C.\ waiting(C) \to \Diamond critical(C)$ (starvation freedom), plus `[delivery]` (every message in flight is eventually delivered) and `[mutex]` | $\forall S,D.\ \square\diamond wf\_recv(S,D)$, $\forall N.\ \square\diamond wf\_check(N)$, $\forall N.\ \square\diamond wf\_exit(N)$ -- **weak fairness only, no compassion** | **LEX** | Single `ranking` call, 5 lexicographic components, no temporal operator in the block. `[00]`/`[01]` drain messages up to the tracked request's timestamp, `[02]` shrinks the set of requests ordered at-or-before it, `[03]` drains what an ahead node still has to consume, `[04]` lets it enter. Weak fairness suffices because Lamport's arbiter *is* the (timestamp, id) order, which is FIFO -- unlike German's. Three obstacles were about decidability, not the protocol: a nullary ghost `last_ts` for Rule-5 finiteness over timestamps, a cached `blocked` flag so `work_helpful` is quantifier-free, and a **nullary** `front` witness (a `node -> node` witness function breaks FAU). See `LIVENESS_CASE_STUDY_LAMPORT.md`. | Yes | Yes | Yes | No |
